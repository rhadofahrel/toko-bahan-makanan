{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "sleep 10 && php artisan migrate --force || true && php artisan serve --host=0.0.0.0 --port=$PORT"
  }
}